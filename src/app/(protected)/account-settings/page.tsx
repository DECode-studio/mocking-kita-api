import AccountSettingsView from '@/src/client/presentation/views/account-settings/AccountSettingsView';

export const metadata = {
  title: 'Account Settings | Mock API Studio',
  description: 'Manage account details, display name, and password settings.',
};

export default function AccountSettingsPage() {
  return <AccountSettingsView />;
}
